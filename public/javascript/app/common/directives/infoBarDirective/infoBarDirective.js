'use strict';

(function(window, undefined) {
    angular.directive('infoBarDirective', function () {
        return {
            restrict: 'AE',
            templateUrl: '/javascript/app/common/directive/infoBarDirective/infoBarDirectiveTmpl.html',

            //directive scope fields
            scope: {

            }
        }
    });
})(window);
